version: '3.8'

services: 

    app:
        build: .
        container_name: flask-app
        ports:
            - 7000:7000 # 7000 -> 7000
        volumes:
          - .:/app
        command: python /app/src/app.py

  